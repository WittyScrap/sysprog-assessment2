#include "types.h"
#include "user.h"

// Currently selected begin mode
static BEGINMODE current = 0;

/**
 * Begins a new graphics command queue.
 * mode defines the modality in which each command is
 * run.
 * 
 */
void begingraphics(BEGINMODE mode) {
    switch(mode) {

    }
}